pipeline {
    agent any

    triggers {
        // Trigger the build when a change is pushed to the repository
        scm('*/5 * * * *') // Poll SCM every 5 minutes (adjust the interval as needed)
    }

    stages {
        stage('Build') {
            steps {
                // Your build steps here
            }
        }

        stage('Notify Slack') {
            steps {
                script {
                    slackSend (
                        color: '#36a64f', // Customize the color
                        message: "Build Status: ${currentBuild.currentResult}", // Include build status
                        channel: '#general', // Specify the Slack channel
                        tokenCredentialId: 'slack-id' // Use your Slack credential ID
                    )
                }
            }
        }
    }
}
